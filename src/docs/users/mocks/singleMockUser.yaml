paths:
  /mockingusers/single:
    post:
      summary: Crea un usuario mock
      description: Create a mock users
      tags:
        - Users Mocks
      responses:
        200:
          description: Retorna un usuario
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Users"
        500:
          description: Error interno del servidor
