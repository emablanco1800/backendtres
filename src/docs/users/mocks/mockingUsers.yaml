paths:
  /mockingusers:
    get:
      summary: Crea 50 usuarios mocks
      description: Create 50 mock users
      tags:
        - Users Mocks
      responses:
        200:
          description: Retorna un array de usuarios
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Users"
        500:
          description: Error interno del servidor
